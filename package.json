{
  "name": "pokedex-local-integration",
  "description": "",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {
    "prisma": "^5.1.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "npx prisma migrate dev --name init"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.1.1",
    "nestjs-prisma": "^0.22.0"
  }
}
